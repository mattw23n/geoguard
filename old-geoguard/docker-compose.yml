version: "3.8"
services:
  geoguard:
    build: .
    volumes: [".:/app"]
    ports: ["8000:8000"]
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - EMBEDDING_MODEL=bge-small-en
      - VECTOR_STORE=faiss
